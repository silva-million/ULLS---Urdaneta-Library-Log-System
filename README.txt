type this sa terminal to run the project:
    python -m venv .venv
    .\.venv\Scripts\Activate.ps1

then install dependencies:
    pip install -r requirements.txt
    pip install flask-migrate
    pip install flask